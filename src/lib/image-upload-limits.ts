// Accept camera originals while keeping multipart requests safely below the host's limit.
export const MAX_IMAGE_SOURCE_BYTES = 25 * 1024 * 1024;
export const MAX_IMAGE_UPLOAD_BYTES = 3 * 1024 * 1024;
